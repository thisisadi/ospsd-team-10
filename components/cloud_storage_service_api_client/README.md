# cloud_storage_service_api_client

Typed client package for calling the HW2 cloud storage FastAPI service.

This package is consumed by `cloud_storage_adapter` and keeps network concerns out of the public API adapter.
